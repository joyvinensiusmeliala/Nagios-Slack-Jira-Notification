Command Name *
slack_middleware_host_notify

Command Line *
/usr/local/nagios/libexec/slack_middleware_host_notify.sh "$NOTIFICATIONTYPE$" "$HOSTNAME$" "$HOSTADDRESS$" "$HOSTSTATE$" "$HOSTOUTPUT$"

Command Type:
misc command
